name = "linear-agent"
main = "worker.js"
compatibility_date = "2024-01-01"

# Secrets (set via wrangler secret put):
# - LINEAR_CLIENT_ID
# - LINEAR_CLIENT_SECRET
# - LINEAR_ACCESS_TOKEN (after OAuth completes)
# - LINEAR_WEBHOOK_SECRET (optional, for verification)
